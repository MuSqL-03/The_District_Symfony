<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        
        <!-- Add your CSS links here -->
        {% block stylesheets %}
        <!-- Example: Link to external CSS or your custom stylesheets -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
        <!-- Include any JS scripts if needed -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>
        {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>

        <!-- Navbar with Category and Plat links -->
        <nav class="navbar navbar-expand-sm bg-danger navbar-dark ">
            <div class="container-fluid">
                <!-- Logo Section -->
                <a class="navbar-brand me-3" href="#">
                    <img src="{{ asset('Images/brand/logo_transparent.png')}}"  alt="Logo" style="width:80px; object-fit: cover;" class="rounded-pill">
                </a>

                <!-- Navbar Toggler for Mobile -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navbar Links -->
                <div class="collapse navbar-collapse" id="collapsibleNavbar">
                    <ul class="navbar-nav mx-auto">

                      <!-- Accueil Link -->
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_accueil') }}">Accueil</a>
                        </li>


                        <!-- Category Link -->
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('categorie_index') }}">Catégories</a>
                        </li>
                        
                        <!-- Plat Link -->
                        <li class="nav-item">
                            <!-- Adjust the ID dynamically as needed -->
                            <a class="nav-link" href="{{ path('app_all_plats') }}">Plats</a>
                        </li>

                        <!-- Contact Link -->
                        <li class="nav-item">
                            <!-- Adjust the ID dynamically as needed -->
                            <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                        </li>
                    </ul>

                   
                </div>
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    {% if app.user %} 
          
                    <li class="nav-item">
                      <a class="nav-link" href="#">Mon compte</a>
                    </li>  
                    
                    <li class="nav-item">
                      <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                    </li>
          
                    {% else  %}
          
                      <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">M'inscrire</a>
                      </li>
          
                      <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Me Connecter</a>
                      </li>
          
                    {% endif %}
                    </ul>
            </div>
        </nav>

        <img src="{{ asset('Images/bg.jpg')}}"  alt="Background Image" style="height:400px; width:100%;"> <!-- Adding some margin to prevent content from being hidden under the navbar -->

        <!-- Main content area -->
            {% block body %}
            <!-- The content of each page will go here -->
            {% endblock %}

         <!-- Footer -->
       <footer class="bg-dark text-center py-3 ">
    <div class="social-icons">
      <a href="#" target="_blank"><i class="fab fa-facebook-f"></i></a>
      <a href="#" target="_blank"><i class="fab fa-twitter"></i></a>
      <a href="#" target="_blank"><i class="fab fa-instagram"></i></a>
      <a href="#" target="_blank"><i class="fab fa-linkedin-in"></i></a>
    </div>
        <p style="color:white;">&copy; 2024 THE DISTRICT. All rights reserved.</p>  
        </footer>

    </body>
</html>
